#!/bin/sh
# pre-push — block จริง ไม่ใช่ขอร้อง (ติดตั้ง: git config core.hooksPath .githooks)
set -e
echo "[pre-push] tsc --noEmit"
npx tsc --noEmit
echo "[pre-push] tasklog-check"
node scripts/tasklog-check.mjs
# เปิดเมื่อพร้อม (ช้ากว่า): npx eslint . --max-warnings 0
echo "[pre-push] OK"
